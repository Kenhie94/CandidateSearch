import "../index.css";

const Nav = () => {
  // TODO: Add necessary code to display the navigation bar and link between the pages
  return (
    <div>
      <h2 className="navbarStyle">
        <a href="/">Home</a>
        <a href="/SavedCandidates">Saved Candidate</a>
      </h2>
    </div>
  );
};

export default Nav;
